# API environment variables — see apps/api/.dev.vars.example for the complete list.
# Copy apps/api/.dev.vars.example to apps/api/.dev.vars and fill in real values.

# Local deploy smoke test token. This must match the API Worker secret API_READINESS_TOKEN.
PEBBLEDESK_API_READINESS_TOKEN=your-database-readiness-probe-token
